<h1>Add a New Post</h1>

<%= form_for @posts do |f| %>
<!--
	<% if @posts.errors.any? %>
		<h2>Errors:</h2>
		<ul>
			<% @posts.error.full_messages.each do |message| %>
				<li><%= messsage %></li>
			<%end%>
	<%end%>
	
		</ul>
		-->
	<p>
		<%= f.label :title %><br />
		<%= f.text_field :title %>
	</p>
	
	<p>
		<%= f.label :content %><br />
		<%= f.text_area :content %>
	</p>
	
	<p>
		<%= f.submit "Add a New Post" %>
	</p>
<% end %>
